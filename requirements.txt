# ============================================================
# 智能数据质量监控平台 - 项目依赖清单
# 安装命令: pip install -r requirements.txt
# ============================================================

# 数据处理核心
pandas>=2.0.0
numpy>=1.24.0

# 数据库连接（用于可视化模块读取 MySQL）
pymysql>=1.1.0
sqlalchemy>=2.0.0

# 可视化
matplotlib>=3.7.0

# 网络请求 & API（AI 模块用）
requests>=2.31.0
openai>=1.0.0

# 环境变量管理（读取 .env）
python-dotenv>=1.0.0

# 大数据处理（PySpark 连接 Hive，可选）
pyspark>=3.4.0

# 工具库
python-dateutil>=2.8.2

# 可选：提升 Pandas 性能（如果提示 PyArrow，可装可不装）
# pyarrow>=14.0.0